import React from 'react'

function ModelOrienation() {

    // Changing Oriention for paddles
    const Orientation = {
        Paddles : {
            rotation: [Math.PI, -Math.PI * 0.5, Math.PI]
        }
    }

    const defaultOrientation = [Math.PI, Math.PI, Math.PI]


    return null
}

export default ModelOrienation